{
  "name": "quickproxyauth",
  "title": "Quick proxy authorization add-on for Firefox",
  "version": "0.0.1",
  "description": "Quick proxy authorization add-on for Firefox",
  "devDependencies": {
    "eslint": "^4.4.1",
    "eslint-config-google": "^0.12.0"
  },
  "scripts": {
    "zip": "zip -r quickproxyauth.xpi . -x '.git/*' -x 'node_modules/*' -x README.md -x 'package*' -x '*.tgz' -x '.*'",
    "test": "eslint ."
  },
  "license": "MIT"
}
